% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_dictionary_auto.R
\name{build_cluster_dictionary}
\alias{build_cluster_dictionary}
\title{Build the cluster dictionary from marker genes}
\usage{
build_cluster_dictionary(
  seu_files,
  sqlite_path = "batch_hashes.sqlite",
  malat1_rank_max = 5,
  top_n = 10,
  remove_programs = c("rod", "cone", "APOE", "low_qual", "MALAT1"),
  write_audit_dir = NULL,
  compare_to = NULL,
  .db_ready = NULL
)
}
\arguments{
\item{seu_files}{Named (by sample id) character vector of base unfiltered
seurat .rds paths (e.g. the \code{all_seu_files} target).}

\item{sqlite_path}{Path to batch_hashes.sqlite.}

\item{malat1_rank_max, top_n}{Passed to \code{\link[=classify_cluster_abbreviation]{classify_cluster_abbreviation()}}.}

\item{remove_programs}{Abbreviations whose clusters get \code{remove = "1"}.}

\item{write_audit_dir}{If non-NULL, write \code{cluster_dictionary_auto.tsv} and
a discrepancy report (vs \code{compare_to}) into this directory.}

\item{compare_to}{Optional path to the previous hand dictionary tsv for the
discrepancy report.}

\item{.db_ready}{Ignored; declare a dependency on the metadata-DB target so
targets runs DB population first.}
}
\value{
Named list of per-sample dictionary tibbles.
}
\description{
Drop-in replacement for \code{read_cluster_dictionary()}: returns a named list of
tibbles (one per sample_id) with columns \code{sample_id}, \code{abbreviation},
\code{gene_snn_res.0.2}, \code{remove}.
}
