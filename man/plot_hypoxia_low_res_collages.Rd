% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypoxia_low_res_collages.R
\name{plot_hypoxia_low_res_collages}
\alias{plot_hypoxia_low_res_collages}
\title{Two-resolution marker-heatmap collages for a low-hypoxia object}
\usage{
plot_hypoxia_low_res_collages(
  low_seu_path,
  nb_paths = NULL,
  clone_simplifications = NULL,
  split_log_dir = "results/hypoxia_cluster_split",
  step = 0.4,
  fallback = c(0.6, 1),
  assay = "SCT"
)
}
\arguments{
\item{low_seu_path}{Path to a \verb{*_hypoxia_low_seu.rds}.}

\item{nb_paths}{Numbat RDS paths (for the clone tree panel); pass \code{NULL} to
omit the clone tree.}

\item{clone_simplifications}{Passed through to \code{\link[=plot_seu_marker_heatmap]{plot_seu_marker_heatmap()}}.}

\item{split_log_dir}{Directory holding \verb{<SRX>_hypoxia_split_log.csv}.}

\item{step}{Resolution increment above the anchor (default 0.4).}

\item{fallback}{Resolutions used when the sample flagged nothing
(default \code{c(0.6, 1.0)}).}

\item{assay}{Assay whose \verb{<assay>_snn_res.<r>} columns are used (default "SCT").}
}
\value{
Character vector of the PDF paths written (\code{NA_character_} per failed
resolution). Never errors — collages are best-effort.
}
\description{
Emits the \verb{*_heatmap_phase_scatter_patchwork.pdf} collage for a
\verb{*_hypoxia_low_seu.rds} at two clustering resolutions: the hypoxia split's
anchor resolution (\code{r_flag}, the one whose clusters were dropped) and
\code{r_flag + step}. If the sample flagged nothing, defaults to \code{fallback} (0.6 and
1.0), so every sample yields a comparable pair.
}
\details{
The anchor is read from the split log's \code{anchor_resolution} column (written by
\code{\link[=split_hypoxia_by_clusters]{split_hypoxia_by_clusters()}}) rather than re-derived, so this cannot drift out
of sync with the split's own stability rule.

Output paths follow \code{\link[=plot_seu_marker_heatmap]{plot_seu_marker_heatmap()}}'s naming with the resolution in
the label segment, e.g.
\code{results/SRX11133592_hypoxia_low_seu.rds__filtered_res0.2_heatmap_phase_scatter_patchwork.pdf}.
}
