% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypoxia_cluster_split.R
\name{pick_hypoxia_anchor}
\alias{pick_hypoxia_anchor}
\title{Pick the anchor (lowest stable) resolution from a sweep's flagged-cell counts}
\usage{
pick_hypoxia_anchor(counts, stability_tol = 0.05, stability_window = 2)
}
\arguments{
\item{counts}{Integer vector of flagged-cell counts, one per resolution,
ordered coarse -> fine. Zeros mean that resolution flagged nothing.}

\item{stability_tol}{Relative tolerance (default 0.05).}

\item{stability_window}{Number of successive resolutions the anchor must agree
with (default 2).}
}
\value{
Integer index into \code{counts}, or \code{NA_integer_} if nothing flagged.
Carries attribute \code{fallback = TRUE} when no resolution stabilised.
}
\description{
Shared by \code{\link[=split_hypoxia_by_clusters]{split_hypoxia_by_clusters()}} (which records the choice in the split
log) and by readers of that log, so the rule lives in exactly one place and
cannot drift between producer and consumer.
}
\details{
Returns the index of the lowest resolution whose flagged-cell count agrees,
within \code{stability_tol}, with the next \code{stability_window} flagging resolutions.
A single successor is not enough: a monotonically rising count can contain a
spurious one-step flat spot (SRX22868102 goes 385, 480, 437, 434, 538, 566 —
0.6/0.8 agree to 0.7\% while the population is still growing), and anchoring
there under-drops by ~20\%. If nothing stabilises, falls back to the
most-cells index (with attribute \code{fallback = TRUE}) so the split never
knowingly under-drops.
}
