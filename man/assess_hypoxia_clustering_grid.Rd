% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions_hypoxia_grid.R
\name{assess_hypoxia_clustering_grid}
\alias{assess_hypoxia_clustering_grid}
\title{Assess hypoxia threshold x clustering resolution for one sample.}
\usage{
assess_hypoxia_clustering_grid(
  hypoxia_seu_path,
  thresholds = c(0.5, 0.4, 0.3),
  resolutions = c(0.2, 0.4),
  assay = "gene",
  plot_path = NULL,
  csv_path = NULL
)
}
\arguments{
\item{hypoxia_seu_path}{Path to a *_seu_hypoxia.rds file (must contain a
hypoxia_score column, and ideally a precomputed SNN graph + UMAP).}

\item{thresholds}{Numeric hypoxia thresholds; low = score <= t, high = > t.}

\item{resolutions}{Numeric Louvain resolutions to cluster each subset at.}

\item{assay}{Assay whose SNN graph is used for clustering (default "gene").}

\item{plot_path}{Output PDF path (auto-derived from sample id if NULL).}

\item{csv_path}{Output CSV summary path (auto-derived if NULL).}
}
\value{
list(plot = \if{html}{\out{<pdf path>}}, summary = \if{html}{\out{<csv path>}}), or NULL on bad input.
}
\description{
Assess hypoxia threshold x clustering resolution for one sample.
}
