% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_dictionary_auto.R
\name{auto_phase_level}
\alias{auto_phase_level}
\title{Auto-generate a cell-cycle phase label per cluster}
\usage{
auto_phase_level(
  seu,
  group.by,
  top_n = 10,
  marker_min = 2,
  s_star_k = 3,
  seurat_assay = "SCT"
)
}
\arguments{
\item{seu}{A Seurat object with \code{S.Score}, \code{G2M.Score}, \code{Phase} in
\code{meta.data} and a \code{group.by} cluster column.}

\item{group.by}{Cluster metadata column name (e.g. "SCT_snn_res.0.6").}

\item{top_n, marker_min}{Marker-rule window and hit threshold.}

\item{s_star_k}{Robust-z multiplier for the s_star outlier fence.}

\item{seurat_assay}{Assay for marker ranking (default "SCT").}
}
\value{
Named character vector mapping each \code{group.by} cluster value to its
phase label. Falls back to modal Phase (or "other") when markers/scores
are unavailable.
}
\description{
Data-driven replacement for the hand-maintained \code{phase} assignments in
\code{data/scna_cluster_order.csv} (which go stale when clustering changes).
For each cluster of \code{group.by}, assigns one phase label by this precedence
(high -> low):
\enumerate{
\item \strong{hsp}    : >= \code{marker_min} of top-\code{top_n} markers are heat-shock genes
\item \strong{pm}     : >= \code{marker_min} of top-\code{top_n} markers are post-mitotic
genes (pm clusters can have high G2M.Score + very low
S.Score, so they are called from markers, not score)
\item \strong{s_star} : cluster mean S.Score is a high-side outlier across clusters
(robust z: median + \code{s_star_k} * MAD of per-cluster means)
\item \strong{g1/s/g2m}: otherwise the modal Seurat \code{Phase} (majority of cells),
lower-cased
}
}
