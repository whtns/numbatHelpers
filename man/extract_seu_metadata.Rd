% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seu_metadata_db.R
\name{extract_seu_metadata}
\alias{extract_seu_metadata}
\title{Extract metadata from a Seurat object and write to SQLite}
\usage{
extract_seu_metadata(
  filepath,
  seu = NULL,
  scna_type = NULL,
  processing_stage = NULL,
  sqlite_path = DEFAULT_DB,
  qc_cols = c("nCount_RNA", "nFeature_RNA", "percent.mt", "nCount_SCT", "nFeature_SCT",
    "log_nCount_gene", "nCount_gene")
)
}
\arguments{
\item{filepath}{Path to the Seurat RDS file. Used as the primary key.}

\item{seu}{Optional Seurat object already in memory. If NULL, read from filepath.}

\item{scna_type}{SCNA type label (e.g. "1q", "2p", "6p", "16q"). Inferred
from \code{filepath} when NULL.}

\item{processing_stage}{Processing stage label (e.g. "debranched", "integrated").
Inferred from \code{filepath} when NULL.}

\item{sqlite_path}{Path to the SQLite database.}

\item{qc_cols}{Character vector of QC numeric columns to summarise.}
}
\value{
Invisibly returns \code{filepath}.
}
\description{
Reads the RDS at \code{filepath} (or uses an in-memory object if \code{seu}
is provided), derives the content hash, and upserts six metadata tables.
The function is idempotent: re-running on the same file updates existing rows.
}
