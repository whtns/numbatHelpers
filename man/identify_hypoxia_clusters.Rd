% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypoxia_cluster_split.R
\name{identify_hypoxia_clusters}
\alias{identify_hypoxia_clusters}
\title{Identify outlier-hypoxia clusters at one grouping}
\usage{
identify_hypoxia_clusters(
  seu,
  grp,
  hypoxia_genes = .hypoxia_marker_set,
  top_n = 20,
  top_markers_n = 5,
  mad_k = 3,
  return_detail = FALSE
)
}
\arguments{
\item{seu}{Seurat object with the grouping column \code{grp} and a \code{hypoxia_score}
metadata column.}

\item{grp}{Name of the cluster grouping column in \code{seu@meta.data}.}

\item{hypoxia_genes}{Character vector of marker genes (default
\link{.hypoxia_marker_set}); used only to populate the informational
\code{n_hyp_markers} / \code{matched_genes} columns.}

\item{top_n}{Window of top markers (by logFC, padj < 0.5) scanned for the
hypoxia-gene overlap columns.}

\item{top_markers_n}{Number of top markers per cluster reported in the
\code{top_markers} column (default 5).}

\item{mad_k}{Robust-z multiplier for the outlier threshold (default 3).}

\item{return_detail}{If \code{TRUE}, return the full per-cluster decision table
(one row per cluster) instead of just the flagged cluster ids.}
}
\value{
By default a character vector of outlier cluster ids (possibly
empty). With \code{return_detail = TRUE}, a data.frame with columns \code{cluster},
\code{n_cells}, \code{top_markers}, \code{n_hyp_markers}, \code{matched_genes}, \code{mean_score},
\code{outlier_fence}, \code{is_outlier}, \code{flagged}. Never errors — returns an empty
result of the requested shape on failure.
}
\description{
A cluster is flagged hypoxia if its mean \code{hypoxia_score} is a high-side
outlier among the per-cluster means, using a robust z rule:
\code{mean_score > median(means) + mad_k * mad(means)} (MAD scaled to be
SD-consistent). This is robust to several elevated clusters because the
median and MAD are dominated by the bulk majority — unlike a Tukey fence,
whose Q3 is inflated by the very clusters it should catch. Flagging is
independent of hypoxia marker-gene presence; the marker columns
(\code{top_markers}, \code{n_hyp_markers}, \code{matched_genes}) are reported for
interpretation only.
}
